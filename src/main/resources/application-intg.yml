spring:
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password: ""
  kafka:
    bootstrap-servers: localhost:9092
    listener:
      concurrency: 1
    producer:
      acks: -1

logging:
  level:
    org:
      elasticsearch:
        client: TRACE
      springframework:
        data:
          elasticsearch:
            core:
              ElasticsearchRestTemplate: DEBUG
        security: DEBUG
        web.socket: DEBUG
        messaging: DEBUG
        web.socket.handler: DEBUG

elasticsearch:
  host-and-port: localhost:9200

redis:
  server: localhost
  port: 6379

#Minio
minio:
  url: http://localhost:9000
  access:
    key: e106
    secret: password
  bucket-name: images